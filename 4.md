## 四、自动化与 GitOps 交付

为了杜绝人工操作的风险，国际大厂的 SRE 均采用 **GitOps** 架构；它的核心理念是将所有的 YAML 文件存放在 Git 代码仓库中，在 Kubernetes 内部部署一个自动化程序，由它实时监听代码仓库的变动，并自动将最新状态同步至集群内。  
这里我们选用 **ArgoCD** 作为核心引擎。  
在终端中依次执行以下命令：

```bash
# 建立专门的命名空间
kubectl create namespace argocd

# 安装 ArgoCD 核心组件
kubectl apply --server-side --force-conflicts -n argocd -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
```

- `--server-side` ：启用 Kubernetes 的服务端应用(Server-Side Apply)，以绕过某些客户端限制，防止 ArgoCD 的某些 CRD （自定义资源定义）文件过大而触发报错；
- `--force-conflicts` ：强制配置管理启用新的所有权，忽略新老所有权之间发生的冲突；  
    安装完成后， Git 代码仓库将成为整个集群状态的“唯一事实来源”； ArgoCD 会每分每秒盯着代码仓库，只要代码有变动，它就会自动把最新状态同步到集群里。  
    在 GitOps 架构下，脱离代码仓库手动修改集群状态会导致 **配置漂移 (Configuration Drift)** ；`kubectl apply` 、 `kubectl edit` 和 `kubectl delete` 等任何试图直接修改集群状态的命令都是严格禁止人工输入的。
